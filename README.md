# Fattoria.by Real Estate Website

Современный сайт агентства недвижимости Fattoria.by с каталогом объектов, формами обратной связи и интеграцией с Google Sheets.

## 🚀 Быстрый запуск

### Требования
- Node.js >= 18.0.0
- MySQL >= 5.7
- npm >= 8.0.0

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd fattoria-website
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте базу данных:**
   - Создайте базу данных MySQL
   - Импортируйте `properties_mysql_dump.sql`
   - Настройте переменные окружения (смотрите `.env.example`)

4. **Запустите сервер:**
   ```bash
   npm start
   ```

Сервер будет доступен на `http://localhost:3000`

## 📁 Структура проекта

```
fattoria-website/
├── api/                    # API эндпоинты и данные
├── css/                    # Стили
├── js/                     # JavaScript файлы
├── includes/               # Шаблоны (header, footer)
├── images/                 # Изображения
├── фото/                   # Фото объектов недвижимости
├── новостройки/            # Страницы новостроек
├── ultra-server.js         # Основной сервер
├── db-mysql-simple.js      # Работа с базой данных
├── bitrix-simple.js        # Интеграция с Google Sheets
└── package.json
```

## 🔧 Конфигурация

### Переменные окружения (.env)

Создайте файл `.env` на основе `.env.example`:

```env
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=fattoria_db
DB_PORT=3306
PORT=3000
NODE_ENV=production
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/...
```

## 🌐 API Эндпоинты

- `GET /api/properties` - Получить все объекты недвижимости
- `GET /api/properties?type=квартира&district=центр` - Фильтры
- `GET /api/property/{unid}` - Получить объект по ID
- `POST /api/submit-form` - Отправка формы обратной связи

## 📧 Формы обратной связи

Формы автоматически отправляют данные в Google Sheets и Gmail.

## 🚀 Деплой на сервер

### На хостинге с Node.js (для fattoria.by)

1. **Создайте директорию для проекта:**
   ```bash
   mkdir -p /home/fattoria/fattoria.by
   cd /home/fattoria/fattoria.by
   ```

2. **Загрузите файлы проекта:**
   ```bash
   # Через git
   git clone <repository-url> .

   # Или через FTP/rsync
   # Загрузите все файлы в /home/fattoria/fattoria.by
   ```

3. **Установите зависимости:**
   ```bash
   npm install
   ```

4. **Настройте переменные окружения:**
   Создайте файл `.env` на основе `.env.example`

5. **Запустите сервер:**
   ```bash
   npm start
   # Или для продакшена:
   npm install -g pm2
   pm2 start ultra-server.js --name "fattoria-website"
   pm2 startup
   pm2 save
   ```

6. **Настройте nginx:**
   Скопируйте `nginx.conf` в `/etc/nginx/sites-available/fattoria.by`:
   ```bash
   sudo cp nginx.conf /etc/nginx/sites-available/fattoria.by
   sudo ln -s /etc/nginx/sites-available/fattoria.by /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl reload nginx
   ```

### Автоматический деплой

Используйте скрипт `deploy.sh`:
```bash
./deploy.sh production
```

### С помощью PM2

```bash
npm install -g pm2
pm2 start ultra-server.js --name "fattoria-website"
pm2 startup
pm2 save
```

## 🔧 Разработка

### Локальная разработка
```bash
npm run dev
```

### Структура базы данных

Импортируйте `properties_mysql_dump.sql` для создания таблиц и данных.

## 📊 Мониторинг

Сервер логирует все запросы и ошибки в консоль. Формы сохраняются в `form-debug.log`.

## 🐛 Устранение неполадок

### Проблемы с базой данных
- Проверьте подключение MySQL
- Убедитесь, что таблицы созданы
- Проверьте переменные окружения

### Проблемы с формами
- Проверьте URL Google Apps Script
- Убедитесь в корректности данных формы

### Проблемы с фото
- Проверьте пути к файлам в папке `фото/`
- Убедитесь в правильности кодировки URL

## 📄 Лицензия

ISC License

## 📞 Контакты

Fattoria.by - Агентство недвижимости в Минске
- Телефон: +375 (44) 702-52-67
- Email: anfattoriya@gmail.com
- Сайт: https://fattoria.by
